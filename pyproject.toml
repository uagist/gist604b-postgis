[project]
name = "postgis-development"
version = "0.1.0"
description = "PostGIS development environment for spatial SQL workflows"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pytest>=9.0.2,<10"
]

[tool.uv]

[tool.pytest.ini_options]
testpaths = ["tests"]
